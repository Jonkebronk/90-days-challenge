import { CatalogCategory, CatalogSchema } from './types'

// Empty - all schemas are now managed via database
export const CATALOG_CATEGORIES: CatalogCategory[] = []

export const CATALOG_SCHEMAS: CatalogSchema[] = []
